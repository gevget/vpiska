# DC Ecosystem Presentation

This application showcases the Deep Creative ecosystem, professional and lifestyle communities.

## Security note

- The current `npm audit` advisory relates to `esbuild`.
- This risk applies specifically to the transition, installation, and build/dev environments.
- The final static site served in the browser does not contain `esbuild`.
- A major migration to Vite 8 has been deferred and will be addressed in a separate task.
